export type ServiceSectionType = {
  title: string;
  image: string;
  description: string;
};

export type TestimonialCardProps = {
  rating?: number;
  desc: string;
  name: string;
  title: string;
  image: string;
};
